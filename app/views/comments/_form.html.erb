<%= form_for(:comment, remote: true) do |f| %>

  <div class="field">
    <%= f.label :body %>
    <%= f.text_area :body, :value => "Great blog!" %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>

<% end %>
