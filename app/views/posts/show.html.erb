<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @post.title %>
</p>

<p>
  <strong>Body:</strong>
  <%= @post.body %>
</p>

<h2>Comments</h2>
<div class="container">
    <%= render @comments, comments: @comments %>
</div>
<br>
<div id="newcomment_elem">      
    <%= render "newcomment", post: @post, comments: @comments %>
</div>
<%= link_to 'Edit', edit_post_path(@post) %> 
<%= link_to 'Back', posts_path %>
